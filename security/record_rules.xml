<odoo>
    <data noupdate="1">

        <!-- Rule cho Sales Target User: chỉ thấy bản ghi của chính họ -->
        <record id="sales_target_user_rule" model="ir.rule">
            <field name="name">Sales Target User: Own Targets</field>
            <field name="model_id" ref="sales_target_omax.model_sales_target"/>
            <field name="groups" eval="[(4, ref('sales_target_omax.group_sales_target_user'))]"/>
            <field name="domain_force">[('salesperson_id', '=', user.id)]</field>
        </record>

    </data>
</odoo>
